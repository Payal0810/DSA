class Solution{
    static long factorial(int N){
        // code here
        
        // base case
        if(N==1 || N==0){
            return 1;
        }
        
        // small calculation
        long rec = factorial(N-1);
        
        // recursive call
        return N*rec;
    }
}
